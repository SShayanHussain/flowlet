import PlaceholderPage from "@/components/placeholder-page";

export default function ConnectionsPage() {
  return (
    <PlaceholderPage
      title="Connections"
      phase="Phase 3"
      description="Connected services and credentials (encrypted at rest). Add or remove generic HTTP and the vertical's key connectors."
    />
  );
}
