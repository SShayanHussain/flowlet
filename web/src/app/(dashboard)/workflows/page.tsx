import PlaceholderPage from "@/components/placeholder-page";

export default function WorkflowsPage() {
  return (
    <PlaceholderPage
      title="Workflows"
      phase="Phase 3"
      description="List, enable/disable, duplicate, and delete your automations. Each row shows last run and success rate."
    />
  );
}
