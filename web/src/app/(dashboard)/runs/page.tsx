import PlaceholderPage from "@/components/placeholder-page";

export default function RunsPage() {
  return (
    <PlaceholderPage
      title="Runs"
      phase="Phase 3"
      description="Run history across all workflows. Open a run to see the step-by-step trace — each node's input/output/latency/status — and retry or replay."
    />
  );
}
